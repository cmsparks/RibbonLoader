' UIHandler.bas
' || VBA file containing all UI related callback
' || functions. Linked to the .officeUI XML
' || elements in order to allow for a dynamic
' || user interface.
Public Sub SpeechHandler()
' Sends a callback to populate the Set Speech
' Document drop down menu.

End Sub
